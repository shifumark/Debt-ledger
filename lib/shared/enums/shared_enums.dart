enum SortOrder { ascending, descending }

enum DebtorSortBy { name, dateAdded, balance, recentlyUpdated }

enum DebtorFilter { all, active, archived }

enum TransactionType { loan, payment, adjustment, forgiveness }

enum AdjustmentDirection { increase, decrease }
